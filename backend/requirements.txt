fastapi>=0.115
uvicorn[standard]>=0.32
sqlmodel>=0.0.22
sqlalchemy>=2.0
alembic>=1.14
python-multipart>=0.0.12
python-dotenv>=1.0
pydantic>=2.11
pydantic-settings>=2.7
pytest>=8.3
# Paraleliza a suite: `-n auto --dist loadfile`. A suite inteira (4825 testes)
# leva ~100 minutos em serie e fechou em 43 com 4 workers — a diferenca entre
# esperar a tarde e esperar o cafe, para quem roda os testes antes de commitar.
#
# Use SEMPRE com `--dist loadfile` (por arquivo), nunca com o padrao `load`
# (por teste): varios arquivos desta suite montam o proprio engine SQLite no
# nivel do modulo e os fixtures sao de escopo de modulo. Por arquivo, cada
# worker fica dono do modulo inteiro — o isolamento que a suite ja assume.
#
# A CI ainda NAO usa: cinco testes so passam por vizinhanca (estado global
# deixado por outro arquivo) e caem quando os arquivos vao para workers
# diferentes. Ver o comentario do job backend-tests em .github/workflows/ci.yml.
pytest-xdist>=3.6
httpx>=0.28
aiosqlite>=0.20
psycopg2-binary>=2.9
openpyxl>=3.1
anthropic>=0.90
pypdf>=5.0
pytesseract>=0.3.13
pdf2image>=1.17
Pillow>=11.0
pywebpush>=2.0
google-auth>=2.35
python-dateutil>=2.9
jinja2>=3.1
xhtml2pdf>=0.2.16
